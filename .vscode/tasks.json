{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "west build (nucleo_f767zi)",
      "type": "shell",
      "options": { "cwd": "${env:USERPROFILE}/zephyrproject" },
      "command": "west build -p always -b nucleo_f767zi -d build ${workspaceFolder}",
      "problemMatcher": ["$gcc"]
    }
  ]
}
